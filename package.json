{
  "name": "nfc-prospecting",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "migrate": "node scripts/migrate-to-supabase.mjs",
    "backfill:city": "node scripts/backfill-city.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "vitest": "^2.0.0"
  }
}
